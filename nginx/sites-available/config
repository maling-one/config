server {
    listen 2443 ssl http2;
    ssl_certificate       /etc/certs/xxxx.crt;
    ssl_certificate_key   /etc/certs/xxxx.key;
    
    include snippets/ssl-params.conf;

    server_name xxxxxxxxxxxxxxxxxxx;
    client_max_body_size 10M;

    access_log off;

    
}
